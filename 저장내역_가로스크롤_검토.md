# 저장내역 가로 스크롤 레이아웃 — 보류된 개선 과제 (2026-09-11)

**상태: 보류.** 사용자 판단으로 위험 대비 이득이 불확실해 지금은 진행하지 않음("이 방법은 위험해 보이니까 일단 숙제로 놔둬"). 나중에 다시 꺼낼 때 이 문서부터 읽고 시작할 것 — 아래 분석을 다시 하지 않아도 되게 정리해둔다.

## 문제

자동구매·번개조합·안티액땜(전체·개별) 4곳 모두 구매내역이 **세로로 카드를 계속 쌓는 방식**이라(`combo_history_ui._render_batches`, `page_auto._render_auto_history_content`), 한 회차 안에서 여러 번 구매하면(실측: 회원 하나가 20건 이상) 화면이 끝없이 길어짐. 회차는 최근 2개로 제한돼 있지만 그 안에서의 건수 제한은 없음.

## 제안했던 방향 — 쿠팡/네이버식 가로 스크롤 카드

- 카드 렌더링이 이미 `combo_history_ui.py` 한 곳에 공유돼 있어서(2026-09-11 통일 작업), 이 모듈의 렌더 컨테이너만 바꾸면 4화면에 한 번에 적용 가능 — 기술적으로는 가능함.
- 최신순으로 가로 배치, `overflow-x:auto` + `scroll-snap`.

## 왜 보류했는가 — 확인된 리스크

1. **가로 스크롤 발견성** — Streamlit엔 OS 스크롤바가 안 보여서, 오른쪽 카드를 일부러 잘리게("peek") 보여주는 등 시각 힌트가 없으면 유저가 더 있는 줄 모르고 지나칠 수 있음.
2. **세로 스크롤과의 충돌** — 페이지 전체가 세로 스크롤인데 그 안에 가로 스크롤 영역이 있으면, 대각선 스와이프에서 기기마다 다르게 반응할 수 있음(이 프로젝트는 실기기 웹뷰 특이 동작 이력이 많음 — 반드시 실기기 확인 필요).
3. **카드 높이 불균일** — 조합 개수에 따라 카드 높이가 다 달라서, 여러 줄로 접는 가로 그리드("부족하면 다음 줄")를 만들면 높이가 안 맞아 지저분해짐. 카드 높이를 고정해야 하는데 그러면 내용이 잘리거나 카드 자체에 세로 스크롤이 또 생길 수 있음.
4. **안티·액땜 짝카드(전체+개별)** — 이미 좌우 2열로 폭을 다 쓰는 구조라, 가로 캐러셀 안에 넣으려면 카드를 크게 줄여야 함(숫자 가독성과 충돌 — "가독성 최우선" 원칙). 폰 폭(~340px)에서는 실제로 한 번에 1~1.3장 정도만 보이는 스와이프가 현실적 한계라, 기대만큼 "쿠팡처럼 여러 개가 한눈에" 보이진 않음.

## 재검토 시 권장안 (진행하기로 결정되면)

- 회차별로 나누지 않고 최신순 카드를 **하나의 가로 스크롤 줄**로(회차 표시는 카드 안에 작게).
- 카드 높이 고정 + 오른쪽 카드가 살짝 잘려 보이게 해서 "더 있음" 힌트.
- 안티·액땜은 좌우 짝 유지하되 카드 폭을 줄이는 대신 숫자 크기는 유지(카드가 세로로 조금 길어지는 방향).
- 2줄 접힘("다음 줄로 밀기")은 리스크 3번이 배로 커지므로 **1줄 가로 스크롤**로 단순화 권장.
- 구현 지점: `combo_history_ui._render_batches` (번개조합·안티액땜 공용) + `page_auto._render_auto_history_content` — 이미 카드 HTML은 통일돼 있으니 컨테이너(감싸는 div/CSS)만 바꾸면 됨.

## 참고

- 저장내역 로그인/패널 통일 작업: 커밋 `6c29796`, `43d4b5b`, `eeb724d`
- 이 검토가 나온 대화 맥락: 회원 105 실측 — 1241회차 자동구매 20건 이상, 세로 목록이 매우 길어짐(스크린샷 근거).
