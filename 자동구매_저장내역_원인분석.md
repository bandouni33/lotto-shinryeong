# 자동구매 "구매내역 안 보임" — 원인 분석 (2026-09-10, 진행 중)

## 증상 (사용자 신고)

- 자동구매 진입 → "저장내역" 클릭 → 로그인 진행 → **구매내역 안 보임**
- **조합시작으로 한 번 구매하면 그때서야** 과거 내역까지 보임
- "유저들한테 사기꾼 얘기 들을 위험", "분쟁소지"

## DB 실측 (Turso 직접 조회, member 105 = 사용자 테스트 계정)

| 항목 | 값 |
|---|---|
| 최신 회차 (`lotto_combinations`) | **1241** |
| member 105 `wallet_db.auto_orders` (status=completed) | **26건, 전부 1241회차** |
| 각 주문의 배정 조합 (`lotto_combinations.auto_order_id IN 7..33`) | **전부 5개씩 정상 존재** (누락 0) |
| member 105 `guest_member_links` | **69개** (guest_id churn 심함) |
| member 105 `guest_auto_orders` (69개 gid 전체) | **0건** — 과거 guest 경로 주문이 이 회원에 하나도 안 묶임 |
| `guest_auto_orders` 전체 | 34건, 1239~1240회차 (구 skip-auth 시절, 현재 회차 아님) |
| `_diag_guest_id_log` 최근 200건 분기 | query 143 / session_state 51 / NEW_MINTED 6 (churn은 많이 개선됨) |

**즉 데이터는 100% 온전하다.** `_collect_purchase_history_items(105)`가 돌기만 하면 26주문 × 5조합 = 130개가 나와야 한다.

## 유력 원인: `_collect_purchase_history_items`가 Turso HTTP 쿼리를 수십 번 순차 실행

`page_auto._collect_purchase_history_items(member_id)` 한 번 호출에 드는 쿼리 수(활성 회원 기준):

1. `history_guest_ids()` → 현재 gid + 링크된 gid 최대 15개
2. 각 gid마다 `list_guest_auto_orders(_gid)` → **약 16회**
3. `list_completed_auto_orders(member_id, limit=20)` → 1회
4. **완료 주문마다** `get_combinations_by_auto_order_id(order_id)` → **약 20회**

합계 **약 37회 순차 Turso HTTP 왕복**. 왕복당 200~500ms + `db_turso.http` 의 간헐적 `KeyError: 'result'` (이 분석 스크립트에서도 재현 — 재시도 12번까지 필요했음).

결과:
- 렌더 하나에 **10~20초** 소요
- 그 사이 모바일 웹소켓 재연결/세션 리셋이 끼면 렌더가 중단 → 빈 화면
- 37개 쿼리 중 하나라도 `KeyError`를 던지면 → `_collect_purchase_history_items` / `_render_auto_history_content` 에 **try/except가 없어** 통째로 예외 → 패널이 비거나 빨간 에러

### 왜 "조합시작 한 번 하면" 보이나

`_append_purchase_history(entry)` 가 방금 산 주문을 **`st.session_state` 에 직접** 넣는다. `_collect_purchase_history_items` 는 1단계에서 세션 항목을 **DB 없이 즉시** 읽는다. 그 직후 렌더는 Turso가 "warm" 하거나 세션 항목이 이미 있어서 통과할 확률이 높다. (근본 해결은 아님 — 세션 항목까지 예외에 휩쓸리면 마찬가지)

## 수정 방향 (다음 세션에서 구현)

1. **조합 조회를 1방 쿼리로 배치** — `get_combinations_by_auto_order_id`를 주문마다 부르지 말고, 대상 order_id 전부 모아 `SELECT ... FROM lotto_combinations WHERE auto_order_id IN (?, ?, ...)` 한 번. guest 경로도 동일.
2. **`_collect_purchase_history_items` 전체를 try/except로 감싸기** — Turso 일시 실패 시 빈 리스트가 아니라 "잠시 후 다시 시도" 안내를 `_render_auto_history_content` 가 구분해서 보여주게(빈 내역과 조회 실패를 구분).
3. **`@st.cache_data(ttl=~15s)` 로 결과 캐시** — 렌더마다 37쿼리 도는 걸 막는다. blink/구매 직후엔 캐시 무효화.
4. `history_guest_ids()` 의 gid 루프 — member 105처럼 링크가 수십 개면 이미 `[:15]` 로 자르지만, member 기준 `auto_orders` 가 이제 주 경로이므로 guest 루프 자체를 "guest 주문이 실제 있는 회원"에만 돌리는 것도 고려.
5. `db_turso` 의 `KeyError: 'result'` — `http.py` 응답에 `result` 키가 없을 때(에러 응답) 재시도 로직을 `_ConnectionWrapper.execute` 레벨에 넣기(현재는 호출자가 각자 감당).

## 함께 요청된 것 (미완)

- **로그인창 통일**: 자동구매/번개조합 저장내역의 로그인 안내가 통합 `login_gate` 배너와 다르게 뜬다는 신고. **안티·액땜조합 저장내역이 "잘 되고 있는" 기준** — 그걸 참조해 통일. (자동구매·번개조합은 `combo_history_ui.render_history_section` 공용인데도 안티액땜과 다르게 보인다면, page_thunder/page_auto 쪽에 별도 로그인 프롬프트가 남아있는지 확인.  `unify-login-prompt-through-login-gate` 메모 참고)
- 저장내역 패널 기본 노출: 로그인 상태면 매 렌더 펼침(`_auto_hist_user_toggled` 없을 때), `combo_history_ui` 에도 동일 적용 — 커밋 `2e9a5fa`. 단 위 성능 문제로 열려도 내용이 안 뜰 수 있음.

## 진단 스크립트

`scratchpad/diag3.py`, `diag4.py`, `diag5.py` (Turso 직접 조회, `.env` 의 `TURSO_DATABASE_URL`/`TURSO_AUTH_TOKEN` 사용, `KeyError` 재시도 포함).
