# 타로점 — 전체 프로세스 통합 정리

`tarot/tarot_page.py`, `wallet_db.py`, `marketing_db.py`에 흩어진 규칙을 한 곳에 모은다. 다른 세 화면(자동구매/번개조합/안티액땜)과 성격이 달라(로또 번호 구매가 아니라 하루 1회 무료 콘텐츠 + 유료 추가) "공통 4대 원칙"은 적용되지 않음 — 별도로 관리. (2026-09-09 작성)

## 1. 진입 조건

시간대 게이트 없음. **로그인 게이트: `render()` 최상단에서 `login_gate()`로 화면 전체를 막음**(2026-09-12부터 — 예전엔 무료 1일 1회는 비로그인으로도 가능했고 유료 추가뽑기에서만 로그인을 요구했으나, 다른 화면과 통일해 시작(카테고리 선택)부터 로그인 필수로 바꿈).

## 2. 하루 무료 1회 제한 — 게스트ID 기반 DB 저장

`MAX_DAILY_DRAWS = 1`. **세션이 아니라 `guest_tarot_draws` 테이블에 guest_id + 날짜로 저장**(2026-08-초 확정) — 예전엔 쿠키 기반이었다가, 안드로이드 웹뷰가 화면 전환마다 새로 생성돼 쿠키가 유실되며 "재접속하면 계속 뽑을 수 있는" 문제가 있어서 지금 방식으로 교체됨.
- `_draws_remaining()`: 오늘 날짜로 롤오버, `get_guest_tarot_draw_count(guest_id, today)`로 복원.
- `_register_draw()`: 카드가 실제로 결정되는 셔플 시점에 소진 처리.
- **이 방식의 유일한 전제조건은 guest_id가 안정적으로 유지되는 것** — 2026-09-09에 발견된 guest_id 유실 문제가 있었을 땐 매번 새 guest_id로 인식돼 "무료뽑기 무한사용"으로 나타났음. 오늘 3중 안정화(내부링크 gid 동봉·카카오 state 복원·localStorage 백업) 적용 후 실제 재현 테스트로 정상 작동 확인 완료.

## 3. 무료 소진 후 — 유료 추가뽑기

- **50P** (`TAROT_EXTRA_DRAW_COST`, `legal_notices.PRICING["tarot_extra_draw"]`).
- `points_notice_dialog("tarot", on_close=_tarot_dialog_close)` 확인 후 진행.
- **2026-09-08 중요 수정**: 예전엔 `deduct_points()` 성공 여부와 무관하게 무조건 추가뽑기를 열어주는 leftover 버그가 있었음(번개조합·안티액땜에서 고친 것과 같은 유형) — 테스트 기간(`_testing_period_active()`)엔 기존 설계대로 취소해도 통과시키는 걸 유지하되, 실카카오 로그인 연동 후에는 **차감 성공 시에만** 진행하도록 정정.
- 적립금 부족 시: 다른 세 화면과 동일한 통합 충전창(`insufficient_balance_dialog`) 적용(2026-09-08).

## 4. 진행 흐름 (UI 단계)

```
1) "지금, 마음이 어디에 머물러 있나요" — 기분 대분류 선택(번아웃/미래불안/외로움/후회미련/사랑연애/인간관계/자존감/반복패턴/화억울함/작은희망 등)
2) 세부 상황 선택
3) 카드 셔플 — 화면을 손가락으로 저어서 진행바를 채움(드래그 제스처, 일정량 이상 저어야 다음 단계)
4) 부채꼴로 펼쳐진 카드 중 한 장 선택 → 카드 확정(이 시점에 _register_draw() 호출)
5) 해석 보기 → 카드 이름·의미·위로 문구 표시
6) 하단 CTA: "자동구매로" / "번개조합으로" 이동 링크
   — 2026-09-09: 이 두 링크도 gid 없이 만들어져 있었던 걸 발견, internal_nav_href로 통일
```

## 5. 게스트ID 안정화

`자동구매_프로세스.md` 8번과 동일하게 3중 적용(내부링크 gid·카카오 state 복원·localStorage 백업). 타로는 특히 **하루 제한 자체가 guest_id 안정성에 직접 의존**하는 유일한 화면이라 이 부분이 가장 민감함.

## 6. 다른 화면과 다른 점 정리

| | 자동구매/번개조합/안티액땜 | 타로 |
|---|---|---|
| 비로그인 이용 | 불가(로그인 필수) | **불가(2026-09-12부터 로그인 필수 — 예전엔 무료 1회 가능했음)** |
| 저장/보관 | 조합을 최근 2회차 보관 | 해당 없음(콘텐츠 소비형) |
| 적중 마킹 | 있음 | 해당 없음 |
| 시간대 게이트 | 자동구매만 있음 | 없음 |

## 조합생성 완료 안내 · 로그인 배너 위치 (2026-09-11, 4화면 통일)

- "조합생성이 완료되었습니다" 계열 안내를 카드 뽑기(⟲) 버튼 바로 밑에 표시(`wallet_ui.render_generation_complete_notice("tarot")`). 문구는 `legal_notices.GENERATION_COMPLETE_NOTICES`에서만 관리 — 자동구매·번개조합·안티액땜·타로 4화면 동일 패턴, 바꿀 땐 거기 한 곳만.
- 로그인 배너("카카오로 시작하기")는 화면 어디서 열려도 항상 최상단으로 1회 자동 스크롤된 뒤 나타난다(`wallet_ui.open_auth_banner`/`render_auth_banner`) — 유저가 버튼을 눌렀는데 배너가 안 보여 혼란스러운 문제 방지.

## 로그인 후 메인화면으로 튕기던 버그 (2026-09-11)

- 카카오 로그인 복귀 페이지 허용 목록(`auth_providers._encode_oauth_state`)에 `"tarot"`도 빠져 있어서 같은 문제가 있었다(안티·액땜과 동일 버그, 같은 커밋으로 수정). 이제 타로 화면에서 로그인해도 원래 화면으로 돌아온다.

## 배포해도 코드가 안 바뀌던 문제 — 화면 모듈 stale import 캐시 (2026-09-12)

- 타로에 로그인 게이트를 추가·배포했는데 몇 분이 지나도 실제 화면엔 반영이 안 됨 — Streamlit Cloud가 git push 배포 때마다 파이썬 프로세스를 매번 완전히 재시작하는 게 아니라서, `import tarot_page`가 `sys.modules`에 캐시된 **예전 코드**를 계속 돌려주고 있었다(`marketing_db.py`의 `_marketing_db()` 워크어라운드와 동일한 원인·증상 — 그때는 marketing_db 하나만 고쳤었음).
- `user_page.py`에 공용 헬퍼 `_reload_if_stale(module)` 추가 — 파일의 실제 mtime이 마지막 로드 시점보다 최신이면 `importlib.reload()`로 무조건 다시 읽는다(안 바뀌었으면 비용 없음). 이 방식으로 import되는 5개 화면 모듈(`page_thunder`/`page_birthday`/`page_hedge`/`page_auto`/`tarot_page`) 전부에 적용 — 앞으로 이 파일들을 고칠 때마다 매번 별도 확인 없이 항상 최신 코드가 반영된다.
- **범위 밖(잠재적 잔여 위험)**: `wallet_ui.py`/`combo_history_ui.py`/`auth_providers.py` 등 코드 곳곳에서 `from X import Y`로 불러 쓰는 다른 공용 모듈들도 이론적으로는 같은 위험이 있을 수 있다 — 다만 오늘 확인된 건 이 5개 화면 모듈뿐이라 우선 이것만 고쳤다. 다른 모듈에서도 "분명히 배포했는데 반영이 안 된다"는 증상이 또 나오면 이 문서를 참고할 것.
