# 기술부채 추적

즉시 처리는 안 하지만 나중에 반드시 다시 봐야 하는 항목들. (네이티브 앱
재빌드가 필요한 항목은 `네이티브_빌드_대기목록.md`에 따로 기록.)

## 1. `st.components.v1.html` deprecation — Streamlit 버전 업그레이드 전 필수 선행 작업

**발견**: 2026-09-17, Streamlit Cloud 앱 로그에 다음 경고가 매 요청마다 반복
됨:
> Please replace `st.components.v1.html` with `st.iframe`.
> `st.components.v1.html` will be removed after 2026-06-01.

이미 명시된 제거 예정일(2026-06-01)이 지난 상태 — 사용 안 하는 죽은 코드가
아니라 **앱 핵심 전반에서 광범위하게 사용 중**인 것으로 확인:
- `wallet_ui.py`, `user_page.py`, `user_scope.py`, `page_thunder.py`,
  `page_hedge.py`, `page_auto.py`, `tarot/tarot_page.py`,
  `frontend/components/haptic.py`, `page_birthday.py`, `premium_test.py`
- 게스트ID 쿠키 동기화, 카카오 로그인 트리거, 번개조합/헤지 UI 렌더링,
  타로 카드 애니메이션 등에 쓰임
- 일부는 "항상 iframe으로 렌더된다"는 sandbox 특성 자체(예: 최상위 문서로의
  네비게이션 제한 회피 기법)에 로직이 의존하고 있어, 단순 이름만
  `st.iframe`으로 바꾼다고 끝나는 게 아니라 **각 사용처의 sandbox/네비게이션
  동작이 동일하게 유지되는지 개별 검증 필요**

**현재 상태(안전)**: 2026-09-17 `requirements.txt`를 `streamlit==1.64.0`으로
고정(P5, 커밋 `e4b3afe`)해뒀기 때문에, 우리가 직접 버전을 올리지 않는 한
이 함수가 갑자기 사라질 일은 없음. 급한 불은 아님.

**선행조건**: **Streamlit 버전을 다음에 올리기 전에, 반드시 위 파일들의
`components.html` 사용처를 전부 조사해서 `st.iframe`으로 전환(또는 대안
검토)하는 작업을 먼저 마쳐야 함.** 순서를 반대로 하면(버전부터 올리면)
앱 전체가 거의 동시에 깨질 위험.

---

## (추가될 항목은 여기 아래에 계속 기록)
