# 자동구매 — 전체 프로세스 통합 정리

여러 파일(`page_auto.py`, `auto_purchase_service.py`, `marketing_db.py`, `wallet_db.py`, `legal_notices.py`, `combo_gen_worker.py`)에 흩어져 있던 자동구매 관련 규칙을 한 곳에 모은다. 자동구매 로직을 건드릴 땐 고치기 전에 이 문서부터 다시 읽고 실제 코드와 대조할 것 — 이 문서 자체가 코드와 어긋나면 그때그때 같이 갱신한다. (2026-09-09 작성)

## 1. 진입 조건 (버튼을 누르기 전에 걸리는 게이트, 순서대로)

| # | 게이트 | 조건 | 실패 시 |
|---|---|---|---|
| 1 | 판매 가능 시간 | **화 09:00 ~ 토 19:55 (KST)**, 일·월 전체 및 화 09:00 이전·토 19:55 이후는 막힘. 2026-09-09부터 `sales_window.py` 공용 모듈로 분리 — **번개조합·안티액땜조합도 동일 규칙 적용됨**(각 문서 참고) | `SALES_WINDOW_BANNER`(구 `AUTO_DEPLOY_WINDOW_BANNER`) 안내문 표시, 구매 진행 안 됨 |
| 2 | 다음 회차 배포용 풀 준비 여부 | 최신 추첨 완료 회차 N 기준 **N+1 회차 조합이 DB에 이미 있어야** 함(주간 조합생성 트리거가 아직 안 돌았으면 없음) | `NEXT_DRAW_POOL_BANNER`("다음회차 조합생성이 완료된 후 이용 바랍니다") |
| 3 | 로그인 여부 | `ensure_member_or_banner()` — 비로그인이면 카카오 로그인 배너(세션당 1회만 노출) | 배너만 뜨고 구매 진행 안 됨 |
| 4 | 적립금 확인 안내 | `points_notice_dialog("auto", ...)` — 세션당 1회만 확인질문, 이후엔 바로 통과 | (안내만, 통과 자체를 막지 않음) |

## 2. 가격

- **10P / 개** (`AUTO_COST_PER_UNIT`, `wallet_db.py`) — `legal_notices.PRICING["auto_per_unit"]`와 항상 같은 값으로 유지해야 함(하나만 고치면 화면 안내문과 실제 차감액이 어긋남).
- 5개 = 50P, 10개 = 100P (선택지는 5/10 두 가지만 제공).

## 3. 구매 확정 시 실제 처리 순서 (`auto_purchase_service.process_auto_purchase`)

```
1) check_next_draw_pool_ready() 재확인 (60초 캐시)
2) 잔액 확인 — 부족하면 "insufficient_balance" 반환
   → 여기서 실패하면 wallet_ui.open_insufficient_balance_dialog()로
     "부족합니다 + 충전하시겠습니까" 통합창 (2026-09-08, 번개조합·안티액땜과 공통)
3) create_auto_order() — 주문 레코드 먼저 생성(상태: 대기)
4) allocate_lotto_combinations_random_sequential() — 아래 4번 참고
5) SMS 큐 등록 — 현재 SMS_ENABLED=False라 실발송 없이 BANNER_ONLY로만 기록
6) deduct_points() — 적립금 실제 차감 (성공해야만 진행, 실패 시 배정 롤백)
7) complete_auto_order() — 주문 완료 처리
   → 여기서 예외가 나면(차감은 이미 성공) `refund_points()`로 **즉시 환불** +
     배정 롤백 + 주문 실패 처리 (2026-09-10, "실패 시 환불" 정책 통일)
```
실패 지점마다 롤백 처리됨: 배정 후 차감 실패 → `release_lotto_combination_allocation()`로 배정 취소 + `fail_auto_order()`. 조합 자체가 부족하면(`InsufficientCombinationsError`) 주문을 실패 처리하고 사용자에게 알림. 차감 성공 후 주문 완료 단계가 깨지면 환불(위 7번).

**실패 시 환불 정책 (2026-09-10, 4개 기능 통일):** 자동구매=배정 실패/완료 예외 시 롤백+환불 · 안티액땜=생성 개수 미달 시 차감 안 함 · 번개조합=`thunder_pending_charges` 미정산 10분 후 자동 환불 · 고급필터=구독 활성화 실패 시 환불.

## 4. 조합 배정 규칙 — 1위/2위/3위그룹 2:2:1 (2026-09-08 최종 확정)

그 회차 격차순위(gap_order) 1~3위 숫자를 기준으로 4차 필터 통과 조합은 정확히 3그룹 중 하나:
- **1위그룹**: 1위 숫자 포함 (2·3위는 있어도 없어도 됨)
- **2위그룹**: 2위 숫자 포함, 1위는 제외
- **3위그룹**: 3위 숫자 포함, 1·2위 둘 다 제외

5개 구매 = 1위그룹2 + 2위그룹2 + 3위그룹1, 10개 구매 = 4:4:2. **매주 10% 추출 시(`combo_gen_worker.py`)와 구매 배정 시(`marketing_db.py`) 반드시 동일 비율**을 써야 함 — 하나만 다르면 한 그룹이 먼저 바닥나 비율이 깨짐. 그룹 내부에서는 `_pick_spread_indices()`로 균등 랜덤 선택(2026-09-07 편향 버그 수정 완료).

## 5. 재고 부족 시 — 회전(rotation)

해당 회차 배포용 풀이 소진되면, 그 회차 배포 상태를 초기화하고 다시 배정을 시도한다(`rotated` 플래그로 표시). 동시 구매 요청 간 경합은 "그 순간까지 미배포 상태인 것만" 원자적 UPDATE로 확정하는 방식(`_claim_pending_ids`)으로 중복 배정을 막음.

## 5-2. 저장내역 패널 · 구매 후 스크롤 (2026-09-10 사용자 신고 수정)

- **저장내역은 로그인 직후부터 기본 노출** — `auto_history_panel_open_6n36s5`를 `setdefault(True)`. 예전엔 접힌 채라 "저장내역" 버튼을 누르거나 조합시작을 한 번 해야(`auto_history_blink`) 열려서 "구매내역이 안 보인다"는 신고로 이어졌다. 번개조합·안티액땜(항상 하단에 렌더)과 통일. "저장내역" 버튼은 이제 접기/펼치기 토글.
- **구매 성공 후 화면 최상단 유지** — `auto_scroll_top` 플래그 → 다음 렌더에서 최상위 문서에 스크립트를 심어 `window.scrollTo(0,0)`. 예전엔 구매 후 화면이 저장내역 패널 하단으로 밀려 홈 버튼이 안 보였다. (저장내역은 사용자가 직접 스크롤해서 확인)
- **카카오 콜백 오류배너 제거** — `handle_oauth_callback`에서 code 교환 실패 시 raw JSON(`카카오 토큰 발급 오류 (400): {...}`)을 빨간 `st.error`로 최상단에 노출하던 것 제거. 카카오 인가코드는 1회용이라 콜백이 두 번 돌면(리런·새로고침) 두 번째는 KOE320으로 실패 → 이미 로그인됐으면 조용히 넘기고, 아니면 짧은 `st.toast`만.

## 6. SMS — 현재 비활성

`sms_sender.SMS_ENABLED = False` (2026-08-29 확정, 알리고 미연동) — 실제 발송 없이 `sms_queue`에 `BANNER_ONLY` 상태로만 기록. `marketing_db.enqueue_sms()`는 이 상태일 때만 전화번호가 비어 있어도 통과하도록 되어 있음(2026-09-09 수정 — 이전엔 검증 누락으로 자동구매가 100% 실패하는 버그가 있었음).

## 7. 세 화면(자동구매/번개조합/안티·액땜조합) 공통 4대 원칙

이 문서는 자동구매 전용이지만, 아래 4개는 세 화면 모두 동일하게 적용돼야 하는 규칙(2026-09-07 확정, `purchase_history_four_rules` 메모 참고):
1. 세 화면 공통 적용 — 화면마다 다르게 가면 안 됨
2. 구매/생성하면 **자동 저장**
3. 추첨 후 **적중내역 자동 마킹**
4. 유저의 구매/저장 기록은 **최근 2회차만 보관**, 다음 회차 진행 시 자동 삭제

자동구매는 4번이 한동안 미충족이었다가(화면 표시만 2회차로 자르고 실제 삭제는 없었음) 수정 완료:
- `marketing_db.cleanup_old_guest_auto_orders(keep_rounds=2)` (게스트)
- `wallet_db.cleanup_old_auto_orders(keep_rounds=2)` (회원)
- 둘 다 `combo_gen_worker.py`의 주간 트리거가 호출.

## 8. 게스트/회원 식별 — 오늘(2026-09-09) 확정된 안정화 체계

자동구매를 포함한 모든 화면의 로그인·구매기록·일일제한이 전부 **guest_id(비로그인 식별자)가 안정적으로 유지되는지**에 달려 있다. 오늘 발견·수정된 3중 안전장치:
1. **내부이동 링크에 gid 쿼리파라미터 동봉** (`user_scope.internal_nav_href`) — 화면 이동(자동구매↔번개조합 등) 시 유실 방지
2. **카카오 로그인 state에 gid 동봉·복원** (`auth_providers._encode_oauth_state`/`handle_oauth_callback`) — 로그인 왕복 중 유실 방지
3. **localStorage 백업** (`user_scope.local_storage_gid_recovery_html`) — Streamlit 웹소켓 재연결로 세션이 통째로 리셋될 때(알려진 Streamlit 플랫폼 이슈) 유실 방지

셋 다 자동구매 화면에도 동일하게 적용되어 있음. 최종적으로는 네이티브 앱 재빌드가 이 문제 자체를 없애준다(AsyncStorage 기반이라 URL/쿠키에 안 걸림).

## 9. 아직 안 끝난 숙제 (`출시전_체크리스트.md` 4번과 연동)

구매 확정 시점에 **실제로 배정된 6개 번호 자체**는 `lotto_combinations`(판매 풀)에만 있고, `auto_orders`는 "몇 개 샀는지"만 기록 — 이 풀은 최근 2회차만 보관되므로, 실결제(PG) 시작 전에 "구매 시점 스냅샷"을 별도 저장해야 함(고객 분쟁 대비). 아직 미착수.
