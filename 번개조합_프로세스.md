# 번개조합 — 전체 프로세스 통합 정리

`page_thunder.py`, `wallet_db.py`, `marketing_db.py`, `legal_notices.py`, `user_scope.py`에 흩어진 규칙을 한 곳에 모은다. 자동구매 문서와 짝을 이루며, 고치기 전에 이 문서부터 대조할 것. (2026-09-09 작성)

## 1. 진입 조건

| 게이트 | 조건 | 실패 시 |
|---|---|---|
| 판매 가능 시간 | **화 09:00 ~ 토 19:55 (KST)** — 자동구매와 동일(2026-09-09 확정, `sales_window.py` 공용 모듈) | `SALES_WINDOW_BANNER` 안내문, "조합시작" 클릭 시 + 다이얼로그 확정 시 이중 체크 |
| 로그인 | `ensure_member_or_banner()` — 세션당 배너 1회 | 배너만 노출 |
| 적립금 확인 안내 | `points_notice_dialog("thunder", game_count=g)` — 세션당 1회 | (안내만) |

## 2. 가격

**10P / 게임** (`THUNDER_COST_PER_GAME`, `calc_thunder_cost`) — 5게임 50P, 10게임 100P.

## 3. 조합 생성 방식 — 서버가 아니라 클라이언트 JS

다른 세 화면(자동구매/안티액땜/타로)과 다르게, 번개조합의 실제 번호 생성은 **`components.html`로 심어진 JS(클라이언트 쪽)**에서 일어난다 — 서버는 `th_save` 네비게이션 전까진 실제 번호를 모른다. 이 때문에 "생성=적립금차감을 한 몸으로 묶는" 처리가 구조적으로 불가능해서, **적립금 차감은 조합시작 확정 시점에 먼저** 하고, 조합은 그 이후 클라이언트에서 생성된다(2026-08-29 확정).

**실패 시 환불 (2026-09-10 확정 — 자동구매/안티액땜/고급필터와 정책 통일):**
- 조합시작 확정 시 `deduct_after_result`로 차감 + `record_thunder_pending(mid, ref, cost, g)`로 **미정산 기록**(`thunder_pending_charges` 테이블, `settled` 0=미정산/1=정산/2=환불).
- 번호가 저장내역에 저장되면(`th_save` 처리부) → `settle_thunder_pending`로 **정산 완료**(환불 대상 제외). 세션 ref 유실 시 그 회원의 가장 오래된 미정산 건 정산.
- 번개조합 화면 진입 시마다 `sweep_stale_thunder_pending(mid)` — **미정산 + 10분 경과** 건은 `refund_points`로 자동 환불(`THUNDER_PENDING_STALE_MINUTES = 10`), 다음 진입 시 안내 토스트.
- 차감을 저장 시점으로 옮기지 않는 이유: JS 연출이 차감 전에 번호를 보여주므로, 저장 시점 차감이면 번호만 보고 이탈해 무한 재생성(무료 재추첨)이 가능. (2026-09-10 사용자 재확인 — `payment-timing-vs-warning-banner` 메모)

- 삭제수 / 고정수 / 행운수 조건을 사용자가 지정.
- **선택값 보존 (2026-09-10 치명적 버그 수정):** `selectedFixed`/`selectedDelete`/`luckyNumbers`는 `components.html` iframe의 JS 메모리(let Set)에만 있었다. "조합시작" → ① 로그인 게이트 `st.rerun()` → ② 적립금 확정창 "확인 후 진행" `st.rerun()` 로 iframe이 **두 번 파기·재생성**되고, 그 뒤 `autoRunCount` 자동 생성이 도는데 그때 iframe은 선택이 빈 새 인스턴스라 **고정수·삭제수가 전부 무시된 채** 조합이 나왔다. → reveal 사이클처럼 `localStorage['thunder_selections_v1']`에 저장하고(매 `toggleNumber`·행운수 탭 최초 로드) 스크립트 로드 시 복원. iframe이 다시 만들어져도 그리드가 선택 상태를 그대로 보여주고, 자동 생성도 그 조건으로 돈다.
- **저장 후 자동 초기화 (2026-09-10 사용자 지시):** `th_save`(저장 완료) 처리부에서 `thunder_clear_selections` 플래그 → 다음 iframe 렌더의 `clear_selections_js`가 true면 `localStorage`의 선택값을 지우고 복원을 건너뛴다. 지난 선택이 남은 걸 모르고 또 생성하는 것 방지.
- **배포용 저장본 우선 배정** (`tryPoolMatch()`, 2026-09-05 구현): 고정수 전부 포함 + 삭제수 전부 미포함인 조합이 배포용 풀(`get_random_pool_combos`)에 있으면 그걸 우선 사용, 부족분만 신규 무작위 생성.
- 결과가 하나씩(2초 간격) 나타나는 연출 — `expectedGameCount`만큼 다 찰 때까지 "결과저장" 링크는 갱신 안 됨.
- **생성 중 화면 유지 (2026-09-10 신고 수정):** 예전엔 게임이 나올 때마다 그 행으로 스크롤을 따라가서, 위로 올려 보려 하면 다음 게임 나오는 순간 다시 끌어내렸다 → **첫 게임에서 한 번만** 결과 영역으로 스크롤(`scrollToResultsIfFirst`), 이후 사용자 스크롤 방임. 그리고 생성 중에는 "⚡ 조합시작" 버튼·게임수 `st.selectbox`를 `disabled=thunder_generating` 처리 — 스크롤하다 버튼을 잘못 누르면 `st.rerun()` → iframe 재생성 → 생성이 통째로 사라지던 문제 방지. 버튼 라벨은 "⚡ 생성 중…". `thunder_approved`가 "생성 중" 플래그: 확정 시 set, `th_save`(저장 완료)·자동생성 아닌 렌더에서 clear.

## 4. 결과저장

- iframe 안에서는 최상위 문서 네비게이션이 sandbox로 막혀 있어, **진짜 `<a>` 링크는 iframe 밖(진짜 Streamlit 영역)**에 두고 iframe이 그 `href`만 실시간 갱신하는 구조.
- 저장 시 `save_guest_generated_combos(guest_id, "thunder", 다음회차, combos)` 호출 → `guest_generated_combos` 테이블.
- 번호 생성이 끝나면 **자동 저장**된다(f183d3b) — "저장 안 되면 여기를 눌러주세요" 링크는 폴백.
- **번호가 끝내 저장되지 않으면**(생성 중 이탈·네트워크 끊김 등) 위 3-2의 자동 환불(10분)으로 되돌려진다.
- 내부이동 링크(`internal_nav_href`)도 이 저장 링크에 gid를 실어보내도록 2026-09-09에 수정 완료(안 그러면 저장 시점에 게스트ID가 바뀌어 저장은 되는데 안 보이는 사고로 이어짐).

## 5. 안드로이드 강제 다크모드 대응 (네이티브 앱 전용, 2026-08-30~09-08 누적)

번개조합은 실기기 화면 이슈가 유독 많았던 화면:
- `:root{color-scheme:light}` CSS로 최상위 문서 강제 다크모드 대응
- `forceDarkOn:false` (WebView 네이티브 옵션)
- **2026-09-08**: 로딩 오버레이가 15% 투명이라 화면 전환 중 프레임이 비쳐 "화면 겹침"으로 보이던 문제 → 완전 불투명 + `androidLayerType:'software'`로 구조적 방지(네이티브 재빌드 대기 중 — [운영 타임라인 아티팩트](https://claude.ai/code/artifact/58b83fb3-98b1-4fc3-88d5-22a3887202f9) 참고)

## 6. 공통 4대 원칙 · 게스트ID 안정화

`자동구매_프로세스.md`의 7·8번과 동일하게 적용됨:
- 자동저장(저장내역 버튼) / 적중 자동마킹 / 최근 2회차만 보관(`cleanup_old_guest_generated_combos(keep_rounds=2)`)
- 적립금 부족 시 통합 충전창(2026-09-08)
- 내부링크 gid 동봉 + 카카오 state 복원 + localStorage 백업 (2026-09-09) 세 겹 게스트ID 안정화

## 7. 아직 안 끝난 것

(현재 없음 — 판매시간대 건은 2026-09-09에 해결됨)

- **저장내역 가로 스크롤 레이아웃 개선 (보류, 2026-09-11)**: 한 회차 안에 구매/생성이 많으면 화면이 끝없이 길어지는 문제 — 쿠팡식 가로 캐러셀을 검토했으나 리스크 판단 후 보류. 상세: `저장내역_가로스크롤_검토.md`.

## 8. 조합생성 완료 안내 · 로그인 배너 위치 (2026-09-11, 4화면 통일)

- "조합생성이 완료되었습니다" 안내를 조합시작 버튼 바로 밑에 표시(`wallet_ui.render_generation_complete_notice("thunder")`). 예전엔 화면 최상단(제목 바로 밑)에 있던 저장완료 토스트를 버튼 밑으로 옮겼다. 문구는 `legal_notices.GENERATION_COMPLETE_NOTICES`에서만 관리 — 자동구매·안티액땜·타로와 동일 패턴.
- 로그인 배너(`카카오로 시작하기`)는 화면 맨 아래(저장내역 등)에서 열려도 항상 최상단으로 1회 자동 스크롤된 뒤 나타난다(`wallet_ui.open_auth_banner`가 스크롤 플래그를 세우고 `render_auth_banner`가 소비) — 유저가 버튼을 눌렀는데 배너가 안 보여 혼란스러운 문제 방지.
