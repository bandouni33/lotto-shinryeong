# 번개조합 — 전체 프로세스 통합 정리

`page_thunder.py`, `wallet_db.py`, `marketing_db.py`, `legal_notices.py`, `user_scope.py`에 흩어진 규칙을 한 곳에 모은다. 자동구매 문서와 짝을 이루며, 고치기 전에 이 문서부터 대조할 것. (2026-09-09 작성)

## 1. 진입 조건

| 게이트 | 조건 | 실패 시 |
|---|---|---|
| 판매 가능 시간 | **화 09:00 ~ 토 19:55 (KST)** — 자동구매와 동일(2026-09-09 확정, `sales_window.py` 공용 모듈) | `SALES_WINDOW_BANNER` 안내문, "조합시작" 클릭 시 + 다이얼로그 확정 시 이중 체크 |
| 로그인 | `ensure_member_or_banner()` — 세션당 배너 1회 | 배너만 노출 |
| 적립금 확인 안내 | `points_notice_dialog("thunder", game_count=g)` — 세션당 1회 | (안내만) |

## 2. 가격

**10P / 게임** (`THUNDER_COST_PER_GAME`, `calc_thunder_cost`) — 5게임 50P, 10게임 100P.

## 3. 조합 생성 방식 — 서버가 아니라 클라이언트 JS

다른 세 화면(자동구매/안티액땜/타로)과 다르게, 번개조합의 실제 번호 생성은 **`components.html`로 심어진 JS(클라이언트 쪽)**에서 일어난다 — 서버는 "결과저장" 버튼을 누르기 전까진 실제 번호를 모른다. 이 때문에 "생성=적립금차감을 한 몸으로 묶는" 처리가 구조적으로 불가능해서, **적립금 차감은 조합시작 확정 시점에 먼저** 하고, 조합은 그 이후 클라이언트에서 생성된다(2026-08-29 확정).

- 삭제수 / 고정수 / 행운수 조건을 사용자가 지정.
- **배포용 저장본 우선 배정** (`tryPoolMatch()`, 2026-09-05 구현): 고정수 전부 포함 + 삭제수 전부 미포함인 조합이 배포용 풀(`get_random_pool_combos`)에 있으면 그걸 우선 사용, 부족분만 신규 무작위 생성.
- 결과가 하나씩(2초 간격) 나타나는 연출 — `expectedGameCount`만큼 다 찰 때까지 "결과저장" 링크는 갱신 안 됨.

## 4. 결과저장

- iframe 안에서는 최상위 문서 네비게이션이 sandbox로 막혀 있어, **진짜 `<a>` 링크는 iframe 밖(진짜 Streamlit 영역)**에 두고 iframe이 그 `href`만 실시간 갱신하는 구조.
- 저장 시 `save_guest_generated_combos(guest_id, "thunder", 다음회차, combos)` 호출 → `guest_generated_combos` 테이블.
- **저장 안 누르고 화면 이탈하면 조합이 사라짐**(경고 문구로 안내) — 차감된 적립금은 복구 안 됨.
- 내부이동 링크(`internal_nav_href`)도 이 저장 링크에 gid를 실어보내도록 2026-09-09에 수정 완료(안 그러면 저장 시점에 게스트ID가 바뀌어 저장은 되는데 안 보이는 사고로 이어짐).

## 5. 안드로이드 강제 다크모드 대응 (네이티브 앱 전용, 2026-08-30~09-08 누적)

번개조합은 실기기 화면 이슈가 유독 많았던 화면:
- `:root{color-scheme:light}` CSS로 최상위 문서 강제 다크모드 대응
- `forceDarkOn:false` (WebView 네이티브 옵션)
- **2026-09-08**: 로딩 오버레이가 15% 투명이라 화면 전환 중 프레임이 비쳐 "화면 겹침"으로 보이던 문제 → 완전 불투명 + `androidLayerType:'software'`로 구조적 방지(네이티브 재빌드 대기 중 — [운영 타임라인 아티팩트](https://claude.ai/code/artifact/58b83fb3-98b1-4fc3-88d5-22a3887202f9) 참고)

## 6. 공통 4대 원칙 · 게스트ID 안정화

`자동구매_프로세스.md`의 7·8번과 동일하게 적용됨:
- 자동저장(저장내역 버튼) / 적중 자동마킹 / 최근 2회차만 보관(`cleanup_old_guest_generated_combos(keep_rounds=2)`)
- 적립금 부족 시 통합 충전창(2026-09-08)
- 내부링크 gid 동봉 + 카카오 state 복원 + localStorage 백업 (2026-09-09) 세 겹 게스트ID 안정화

## 7. 아직 안 끝난 것

(현재 없음 — 판매시간대 건은 2026-09-09에 해결됨)
