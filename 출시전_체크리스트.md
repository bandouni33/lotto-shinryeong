# 로또신령 정식 출시 전 체크리스트

작업하면서 나온, 정식 출시 전에 반드시 처리해야 할 항목들을 놓치지 않기 위한 기록.

## 1. 기존 테스트 계정 포인트 잔액 리셋
- **문제**: 카카오 실연동 전(mock 인증만 쓰던 시기)엔 "Mock 결제" 버튼으로 제한 없이 포인트를 충전할 수 있었음 — 일부 테스터는 100만원 상당까지 충전해본 적 있음.
- **왜 문제인가**: 이 상태로 정식 출시하면, 그 시절 테스터들만 실제 결제 없이 큰 포인트를 들고 시작하게 돼서 신규 유저와 형평성이 안 맞음.
- **처리 시점**: 토스페이먼츠 실제 결제 연동이 완료되는 시점(`pg_configured()`가 켜지는 순간) 직전/직후.
- **처리 방법**: 로그인 연결(카카오 계정-회원 매핑, `guest_member_links`)은 그대로 두고, **기존 회원 전체의 지갑 잔액(`wallets.balance`)만 신규가입 보너스(`SIGNUP_BONUS = 500P`)로 일괄 리셋**. 필요하면 `admin_dashboard.py`에 관리자 실행 버튼으로 만들거나, 1회성 스크립트로 처리.
- 관련 코드: `wallet_db.py`(SIGNUP_BONUS, wallets 테이블), `wallet_ui.py`(`charge_dialog`, `_testing_period_active`)

## 2. 100회/200회 조합생성 자동화 (엑셀 재계산)
- `조합생성_후보숫자_추적표_샘플.xlsx`의 "1차필터" 시트, 7행/484행이 매 회차 자동으로 채워지도록 하는 작업. 복잡한 재계산 로직이 필요해 별도 세션에서 정밀하게 진행하기로 미룸(2026-09-05경 논의).

## 3. `combo_filter_rules_stage1/2.json`을 DB로 이전
- 원래 보안 우려(경쟁사 등에 필터 로직 노출)로 GitHub 저장소를 비공개 전환했다가, Streamlit Community Cloud 무료 티어의 배포 파이프라인이 완전히 망가지는 걸 확인(2026-09 초) — 저장소는 반드시 Public이어야 함이 확정됨.
- 따라서 이 파일들의 보안은 "저장소 비공개"가 아니라 **저장소에서 완전히 빼서 DB(Turso)로 옮기는 방식**으로 해결해야 함. 아직 미착수.
