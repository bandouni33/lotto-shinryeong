streamlit==1.64.0
requests==2.34.2
python-dotenv==1.2.3
pandas==3.0.5
numpy==2.5.3
pyxlsb==1.0.10
openpyxl==3.1.5

libsql-client==0.3.1

# Google Play 인앱결제 서버 검증(google_play_pg.py) — 서비스 계정 JWT 서명/토큰 발급용.
# google-api-python-client 등 무거운 SDK는 안 쓰고 REST 직접 호출 + 이 라이브러리로
# 인증 토큰만 발급받는다(requests는 위에 이미 있음).
google-auth==2.58.1
