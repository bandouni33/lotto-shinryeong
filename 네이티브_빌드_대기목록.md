# 네이티브 앱 빌드 대기 목록

서버(Streamlit) 코드는 git push만으로 몇 분 내 반영되지만, 아래 항목들은
`LottoShinryeong/`(React Native/Expo) 쪽 코드 변경이라 EAS Build로 새로
빌드해서 앱스토어(구글플레이/애플)에 재제출해야 실제 유저에게 반영된다.
이 프로젝트는 Expo OTA 업데이트(EAS Update)가 설정돼 있지 않아
(`app.json`에 `runtimeVersion`/`updates` 없음, `expo-updates` 패키지 미설치)
JS/TS만 바꾸는 변경도 전부 이 경로를 거쳐야 한다. 여러 건을 모아서
한 번에 빌드하기 위한 기록.

## 1. 카카오 간편인증 — WebView 재생성 2회 → 1회 (2026-09-16 진단, 아스트라)

**대상 파일**: `LottoShinryeong/components/streamlit-webview.tsx`

**현재 문제**: `uri`가 렌더마다 파생되는 값이고 `key={uri}`라서, 카카오
로그인 성공 시 토큰을 URL에 실어 웹뷰를 통째로 재생성(1회) → 3초 뒤
토큰을 지우며 또 재생성(2회, 완전 낭비 — 토큰은 이미 서버가 1회용으로
소비·삭제함). 이 두 번째 재생성이 막 세운 로그인 세션을 다시 깨뜨려서
"로그인은 성공했는데 내정보에 반영 안 됨" 계열 버그의 근본 원인으로
추정(핀치줌 사라짐 버그와도 연관 — 아직 100% 확정은 아님, 실기기 검증 필요).

**계획(A안, 최소변경)**:
- `uri`를 렌더 파생값 대신 `setUri()`로 명시적으로만 변경하는 상태로 전환
  → 재로드 2회→0회(토큰 로드 1회만 발생, `onLoadEnd`에서 상태를 건드릴
  이유 자체가 없어짐)
- 로그인 로드에도 `_cb` 캐시버스터 부착(지금은 `fresh_start` 경로에만 있음)
- 서버 쪽(`user_page.py` 인근)에서 `kakao_native_trigger` 쿼리 파라미터
  정리 — 지금은 아무도 안 지워서 나중에 안 눌렀는데 카카오 로그인창이
  다시 뜰 수 있는 부작용 있음
- 규모: `streamlit-webview.tsx` 한 파일, 대략 15~25줄

**검증 방법**: 실기기에서 `onLoadStart` 횟수 로그(또는 기존 진단 배너)로
로그인 시 실제 네비게이션이 1회인지 확인. 코드만으론 리런 횟수는 알아도
실제 웹뷰 네비게이션 횟수는 확인 불가.

**부작용으로 같이 해결되는 것**:
- 뒤로가기로 로그인 토큰 URL이 히스토리에 남아 로그아웃 후에도 재로그인되는 문제
- 토큰 로드에 캐시버스터가 없어 캐시 걸리면 로그인이 조용히 실패하는 문제

---

## 2. 로그인 후 "보던 화면 그대로" 복귀 (2026-09-26, 커밋 `15b7e47a`)

**대상 파일**: `LottoShinryeong/components/streamlit-webview.tsx`

로그인 성공 시 `buildUri()`로 주소를 재조립해 **메인화면으로 튕기던** 문제 —
지금은 `reloadWith({ native_kakao_token: ..., _cb: ... })`로 "지금 보고 있던
주소 + 토큰"으로 바꿔 두었다. 서버 쪽 재개(배너를 열 때 서버에 의도를 남기고
로그인 완료 때 되살리는 경로)는 완료 — `auth_providers._remember_pending_resume`
→ `finalize_login` → `wallet_ui._finish_auth_success`.
**앱 빌드가 올라가야 실기기에서 화면이 유지된다** — v54는 여전히 메인으로 튕긴다.

검증: `tests/test_resume_and_experiment.py`(네이티브 토큰 경로 진입점 조립 검증 포함),
`tests/test_iap_native_branch.py`의 N10(reloadWith 계약)·N11.

---

## 3. 안드로이드 하드웨어 뒤로가기로 다이얼로그 닫기 (2026-09-23)

**대상 파일**: `LottoShinryeong/components/streamlit-webview.tsx` (`handleNativeBack`)

서버 쪽 신호는 이미 배포됨 — `wallet_ui.inject_manual_dialog_back_bridge()`가
`[data-testid="stDialog"]`(내정보·사용설명서 등 모든 st.dialog 공통) 열립/닫힘을
`{type:'dialogState'}`로 앱에 보낸다. 네이티브가 그 신호로 뒤로가기를 가로채
다이얼로그를 닫는 처리가 이 빌드에 실려야 "다이얼로그가 열려 있어도 뒤로가기가
앱을 종료해버리는" 문제가 사라진다.

---

## 4. 번개조합 화면 전환 로딩 오버레이 불투명 처리 (2026-09-08)

**대상**: 네이티브 웹뷰 로딩 오버레이(`androidLayerType: 'software'` 포함)

15% 투명이라 화면 전환 중 프레임이 비쳐 "화면 겹침"으로 보이던 문제 —
`번개조합_프로세스.md`에 기록된 대로 네이티브 재빌드가 필요하다(JS만으로는 해결 안 됨).

---

## 5. 릴리스 빌드 난독화(R8/ProGuard) — 스토어 제출 전

`출시전_체크리스트.md`에 적어둔 항목을 빌드 때 같이 처리한다. 서버 비밀값은
클라이언트에 안 실려 있지만, 난독화 없이 올리면 디컴파일로 필터 규칙·API 호출
구조가 그대로 노출된다. 제출 전 `minifyEnabled true` + R8 규칙 적용 여부부터 확인.

---

## 참고 — 빌드가 필요 **없는** 항목 (서버 배포만으로 반영)

헷갈리기 쉬워 따로 적어둔다. 아래는 전부 Streamlit(서버) 쪽 코드라 git push 후
몇 분 안에 실기기 화면에도 그대로 반영된다 — **빌드를 기다릴 필요가 없다.**

| 항목 | 상태 |
|---|---|
| 저장 직후 최신 카드 2~3회 깜빡임 연출 (`combo_history_ui` + `page_auto`) | 커밋 `ad53ad3d` — 푸시 승인 대기 |
| 자동조합 '확인 후 진행'을 로그인 없이 눌렀을 때 안내문구 (`page_auto.py`) | 푸시 완료 `17c7b555` |
| 타로 추가뽑기 확인을 로그인 없이 눌렀을 때 안내문구 (`tarot/tarot_page.py`) | 커밋 `f13f9157` — 푸시 승인 대기 |
| 테스터 임시 충전 스위치 `wallet_ui.TEST_CHARGE_ENABLED = True` | 푸시 완료 `cdeb7218` — **심사 제출 전 `False`로 내릴 것** |

---

## (추가될 항목은 여기 아래에 계속 기록)
