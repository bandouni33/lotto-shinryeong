# 네이티브 앱 빌드 대기 목록

서버(Streamlit) 코드는 git push만으로 몇 분 내 반영되지만, 아래 항목들은
`LottoShinryeong/`(React Native/Expo) 쪽 코드 변경이라 EAS Build로 새로
빌드해서 앱스토어(구글플레이/애플)에 재제출해야 실제 유저에게 반영된다.
이 프로젝트는 Expo OTA 업데이트(EAS Update)가 설정돼 있지 않아
(`app.json`에 `runtimeVersion`/`updates` 없음, `expo-updates` 패키지 미설치)
JS/TS만 바꾸는 변경도 전부 이 경로를 거쳐야 한다. 여러 건을 모아서
한 번에 빌드하기 위한 기록.

## 1. 카카오 간편인증 — WebView 재생성 2회 → 1회 (2026-09-16 진단, 아스트라)

**대상 파일**: `LottoShinryeong/components/streamlit-webview.tsx`

**현재 문제**: `uri`가 렌더마다 파생되는 값이고 `key={uri}`라서, 카카오
로그인 성공 시 토큰을 URL에 실어 웹뷰를 통째로 재생성(1회) → 3초 뒤
토큰을 지우며 또 재생성(2회, 완전 낭비 — 토큰은 이미 서버가 1회용으로
소비·삭제함). 이 두 번째 재생성이 막 세운 로그인 세션을 다시 깨뜨려서
"로그인은 성공했는데 내정보에 반영 안 됨" 계열 버그의 근본 원인으로
추정(핀치줌 사라짐 버그와도 연관 — 아직 100% 확정은 아님, 실기기 검증 필요).

**계획(A안, 최소변경)**:
- `uri`를 렌더 파생값 대신 `setUri()`로 명시적으로만 변경하는 상태로 전환
  → 재로드 2회→0회(토큰 로드 1회만 발생, `onLoadEnd`에서 상태를 건드릴
  이유 자체가 없어짐)
- 로그인 로드에도 `_cb` 캐시버스터 부착(지금은 `fresh_start` 경로에만 있음)
- 서버 쪽(`user_page.py` 인근)에서 `kakao_native_trigger` 쿼리 파라미터
  정리 — 지금은 아무도 안 지워서 나중에 안 눌렀는데 카카오 로그인창이
  다시 뜰 수 있는 부작용 있음
- 규모: `streamlit-webview.tsx` 한 파일, 대략 15~25줄

**검증 방법**: 실기기에서 `onLoadStart` 횟수 로그(또는 기존 진단 배너)로
로그인 시 실제 네비게이션이 1회인지 확인. 코드만으론 리런 횟수는 알아도
실제 웹뷰 네비게이션 횟수는 확인 불가.

**부작용으로 같이 해결되는 것**:
- 뒤로가기로 로그인 토큰 URL이 히스토리에 남아 로그아웃 후에도 재로그인되는 문제
- 토큰 로드에 캐시버스터가 없어 캐시 걸리면 로그인이 조용히 실패하는 문제

---

## (추가될 항목은 여기 아래에 계속 기록)
