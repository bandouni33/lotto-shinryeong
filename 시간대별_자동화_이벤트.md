# 로또신령 — 시간대별 자동화 이벤트 모음

여러 파일에 흩어져 있어서 하나가 조용히 스킵돼도 눈치채기 어려운 "시간/요일 기반" 로직을 한 곳에 모아둔다. 새로 추가되는 시간 기반 자동화는 반드시 여기에도 기록할 것. (2026-09-06, 1241회차 조합생성 트리거가 조건 버그로 조용히 스킵된 사건을 계기로 작성)

## ⚠️ 2026-09-13 확정(사용자 지시, 최종) — 4차 필터/추출/배포 로직 대개편 (1242회차부터 적용)

기존(2026-09-08까지) "상중하 0~4개, 상위1~3위 중 최소1개, 10% 추출, 1위/2위/3위그룹 2:2:1 배분"을 아래로 전면 교체. (중간에 top1~6위·6단계(2:2:2:2:1:1) 방식을 검토·구현했었으나, 구매묶음 5개와 그룹 수가 안 맞아 "랜덤 교차" 같은 특수 로직이 필요했음 — 동시구매 경합까지 얽힌 `marketing_db.py`의 위험을 낮추기 위해 같은 날 **top1~5위·5단계(1:1:1:1:1)로 최종 단순화**. 최근100회 출현빈도 데이터로 5위/6위 사이 자연스러운 단절 확인 후 결정.)

1. **4차필터 조건 변경**: 상중하 각 **1~4개**(0개 불가로 강화) / 상위**1~5위** 중 최소1개 포함(1~3위에서 확대)
2. **추출 비율**: 4차 통과 전체의 **10%→5%**로 변경, **1~5위그룹 각 20%씩 균등**(정수비 1:1:1:1:1)로 배분
3. **구매 배포(1묶음=5개) 배분**: 1~5위그룹 **각 1개씩**(5개묶음에 5개그룹이 정확히 1:1 대응 — 랜덤 교차 같은 특수 로직 불필요)

1242회차 최종 검증(top1~5, anchor=1241 기준): 4차 통과 **1,064,755개**, 5% 추출 목표 **53,238개**(1위34/2위14/3위45/4위33/5위40 — 5개 티어 각 ~10,648개씩 균등 배분, 실제 배포후보 파일 `1242회차_top1-5위_5퍼센트_배포후보.csv`로 이미 전달·저장 완료).

**코드 반영 현황 — 3개 파일 모두 완료**:
- ✅ `combo_filter_v2.py` — 4차 조건(상중하1~4, top5) 수정 완료, `generate_next_round_combos()` stats에 `top5_numbers` 추가(하위호환으로 `top3_numbers`도 유지). 독립 실행으로 `final_count=1,064,755` 재검증 완료.
- ✅ `combo_gen_worker.py` — `EXTRACT_RATE=0.05`, `RANK_TIER_RATIO=(1,1,1,1,1)`(5단계), `_rank_tier()`/`extract_sample_by_rank_tier_ratio()` 5단계로 확장, `main()`의 marketing_db 호출부도 `top5_numbers` 기준으로 전환 완료.
- ✅ `marketing_db.py` — **완료**. `lotto_combinations.top3_mask`는 정수 컬럼이라 스키마 변경 없이 5비트(1,2,4,8,16)로 확장. `draw_generation_stats` 테이블은 `rank4_num`/`rank5_num` 컬럼을 `_migrate_draw_generation_stats()`(자동 ALTER TABLE, 기존 `_migrate_lotto_combinations()`와 동일 패턴)로 추가 — **앱이 다음번 DB 연결 시 자동 마이그레이션, 수동 SQL 불필요**. `_compute_top3_mask`/`bulk_insert_lotto_combinations`/`record_draw_generation_stats`/`get_draw_generation_stats`/`allocate_lotto_combinations_random_sequential`(`range(3)`→`range(len(RANK_TIER_RATIO))`) 전부 5단계로 전환, 마스크↔티어 라운드트립 및 3개 파일 `py_compile` 문법검사까지 완료.
- ⚠️ **로컬 파일만 수정됨 — 실제 배포(git push)는 사용자가 직접 진행해야 함**(이 세션은 GitHub 저장소에 직접 push할 수 없음, `lotto-app` 폴더 파일만 갱신됨). push 후 앱이 재시작/재연결되는 시점에 `draw_generation_stats` 테이블 마이그레이션이 자동으로 실행됨.

## 📌 고정 참고 — 적립금/배포방식 (2026-09-08 확정, 2026-09-13 배분비율 변경 — 위 항목 참고)

| 화면 | 필요 적립금 | 배포방식 |
|---|---|---|
| 자동구매 | 5개 **50P**(10P/개) | 4차 필터 통과 배포용 저장본에서 **1~5위그룹 각 1개씩**(2026-09-13 최종 변경, marketing_db.py까지 반영 완료) |
| 번개조합 | 5개 **50P**(10P/게임) | 고정·삭제·행운수 조건 그대로 적용. 배포용 저장본에 조건 일치 조합 있으면 우선 배정, 부족분은 신규생성 — **이미 구현됨(2026-09-05, `tryPoolMatch()`)** |
| 전체·개별리셋(안티/액땜) | 5개 선택 시 **100P**(선택수×2×10P — 개별+전체 동시 생성) | 스캔번호 반대조건 그대로 적용. 배포용 저장본에 조건 일치 조합 있으면 우선 배정, 부족분은 신규생성 — **이미 구현됨(2026-09-05, `_pool_priority_matches()`)** |

- 2026-09-08 정정: `wallet_db.AUTO_COST_PER_UNIT`이 100P로 잘못 들어가 있었음(사용자 확인: 실제는 10P/개, 번개조합과 동일) — 10으로 정정, `legal_notices.py`의 PRICING/안내문구도 동기화 완료.
- 2026-09-08 정정: "배포용 저장본 우선 배정"을 "미구현"으로 잘못 판단했었음 — 실제로는 번개조합(`page_thunder.py`의 `tryPoolMatch()`)과 전체/개별리셋(`page_hedge.py`의 `_pool_priority_matches()`) 둘 다 2026-09-05에 이미 구현·연결돼 있었고, 격리 테스트로 필터링 로직도 정확함을 확인함. 검색어가 부정확해서(파일마다 함수명이 다름) 처음에 못 찾았던 것.

## 로그인 배너 / 적립금 안내창 — 세션당 1회만 노출 (2026-09-08 확정)
구매·내정보·충전 등에서 로그인이 필요할 때 뜨는 배너, 그리고 구매 시 뜨는 "적립금 이용 안내"창 — 둘 다 이전엔 시도할 때마다 매번 다시 떴음. **이제 앱을 닫기 전까지(세션 1회) 딱 한 번만 뜨고, 이후엔 자동으로 통과**시키도록 수정:
- 로그인 배너: `wallet_ui.ensure_member_or_banner()`의 `AUTH_BANNER_SEEN_ONCE` 플래그 — 자동구매/번개조합/안티액땜/타로/admin_filter가 전부 이 함수를 거치므로 한 곳 수정으로 전체 적용됨. "👤 내정보" 버튼은 이 함수를 안 거치고 직접 배너를 열고 있어서 별도로 놓칠 뻔했음 — 같은 플래그 공유하도록 수정 완료.
- 적립금 안내창: `wallet_ui.points_notice_dialog()`의 `POINTS_NOTICE_SEEN_ONCE` 플래그 — 2회차부터는 확인 질문 없이 바로 진행. 단, 실제 적립금이 부족하면 각 화면의 실패 안내("적립금이 부족합니다" 등)는 그대로 매번 뜬다 — 확인 질문만 줄인 것.

| # | 이벤트 | 조건(요일/시각) | 실행 방식 | 관련 파일 |
|---|---|---|---|---|
| 1 | 당첨번호 자동동기화 | **방문자 무관, 매주 토요일 22:00·23:00 KST(=13:00·14:00 UTC) 딱 2번만** — 2026-09-07 22:19 최초 도입(그때는 매시 정각) → 같은 날 22:42 "추첨이 토요일 20:30뿐"이라는 이유로 토요일 2번으로 축소(현재 값) | GitHub Actions 스케줄(`.github/workflows/hourly_draw_sync.yml`)이 `hourly_draw_sync.py` 실행 → `sync_latest_from_dhlottery()`, 실패 시 30초 간격 최대 3회 재시도. **2026-09-13 재확인**: 저장소 시크릿은 등록돼 있었으나 `TURSO_DATABASE_URL` 값 자체가 잘못 들어가 있어(`LibsqlError: URL_SCHEME_NOT_SUPPORTED`) 9/7 테스트 실행 3회가 전부 실패했고, 그 결과 9/12(토) 예약 실행도 단 한 번도 발생한 기록이 없었음(실제 확인된 자동 실행 이력 0건 — 지금까지의 실행은 전부 수동 실행뿐이었음) — 사용자가 GitHub Settings에서 시크릿 값을 `.env`와 동일하게 재입력. **2026-09-13 정정**: 재입력 후 정상화 확인 완료 — 1241회차가 앱에 정상 자동 반영됨(사용자 확인) | `hourly_draw_sync.py`, `.github/workflows/hourly_draw_sync.yml`, `draw_results_db.py` |
| 2 | 다음 회차 조합 생성 트리거 | **매주 일요일 14:00~14:59 KST**, 30분 쿨다운, 누군가 앱 방문 시 체크 | `subprocess.Popen`으로 `combo_gen_worker.py` 백그라운드 실행(3~5분 소요). 4차 필터 통과 조합 중 5%를 **1~5위그룹 각 20%(1:1:1:1:1) 균등 비율**로 추출·저장(아래 "핵심 개념" 참고, 2026-09-13 최종 변경). 회차별 "확인용" 로컬 사본을 `lotto-app/{회차}회차/조합_확인용.csv`로 별도 저장(배포용 Turso 저장본과 별개, git 미포함) | `combo_gen_trigger.py`(트리거), `combo_gen_worker.py`(실제 생성 + `RANK_TIER_RATIO`, `extract_sample_by_rank_tier_ratio`, `save_local_verification_copy`), `app.py`(진입점) |
| 3 | 판매(배포) 가능 시간 | **화요일 09:00 ~ 토요일 19:55 KST** — 자동구매·번개조합은 이 시간 외엔 구매/조합생성 자체가 막힘. 2026-09-09부터 3개 화면(자동구매·번개조합·안티액땜조합) 전부에 동일 적용했었으나, **2026-09-14(사용자 지시)로 안티·액땜조합(page_hedge.py)만 이 제한에서 제외** — 이 화면은 요일·시간과 무관하게 언제든 적립안내→확인→차감·조합생성 진행 | `sales_window.py`의 `is_sales_window_open()`을 자동구매·번개조합만 호출·확인(page_hedge.py는 2026-09-14부로 호출 제거) | `sales_window.py` (공용), `page_auto.py`/`page_thunder.py` (호출부) — `page_hedge.py`는 더 이상 호출 안 함 |
| 4 | 구매조합 배분(1묶음=5개) | 시간 조건 아님(참고용) — 5개 구매 단위마다 **1~5위그룹 각 1개씩**(10개면 각 2개씩, 2026-09-13 최종 변경) | 구매 시점에 `allocate_lotto_combinations_random_sequential()` 호출. 2026-09-07 편향 버그 수정: `_pick_spread_indices`가 그룹 내 앞쪽을 뒤쪽보다 최대 6배 더 자주 뽑던 편향을 시뮬레이션으로 확인 후 `random.sample` 기반 균등추출로 교체 | `marketing_db.py`(`RANK_TIER_RATIO`, `_rank_tier_quota`, `_pick_bundle_candidates`) |

### 핵심 개념: 1위~5위그룹 정의 (2026-09-13 최종 확정 — 기존 3그룹 방식에서 5그룹으로 전면 개편)
그 회차의 격차순위(gap_order) 1~5위 숫자(예: 1위=34, 2위=14, 3위=45, 4위=33, 5위=40)를 기준으로, 4차 필터를 통과한 모든 조합은 아래 5개 그룹 중 **정확히 하나**에만 속한다(서로 안 겹치고, 합치면 전체와 같음, 우선순위 1위>2위>3위>4위>5위):
- **1위그룹**: 34 포함 (2~5위 숫자는 있어도 되고 없어도 됨)
- **2위그룹**: 14 포함, **1위(34)는 제외**
- **3위그룹**: 45 포함, **1·2위 둘 다 제외**
- **4위그룹**: 33 포함, **1~3위 전부 제외**
- **5위그룹**: 40 포함, **1~4위 전부 제외**(4차 조건상 이 시점엔 5위가 반드시 포함돼 있음)

이 5그룹 비율(1:1:1:1:1, 각 20%)은 **① 매 회차 5% 추출 시**(위 표 #2)와 **② 구매 시 배정 시**(위 표 #4) 양쪽에 **똑같이** 적용돼야 한다 — 둘 중 하나만 다른 비율이면 특정 그룹이 먼저 바닥나 "정확히 배정" 원칙이 깨진다. (2026-09-08엔 "1위/2위/3위그룹 2:2:1"이었고, 2026-09-13 당일 한때 "1~6위·2:2:2:2:1:1"도 검토했으나 구매묶음 5개와 안 맞아 랜덤교차 로직이 필요해지는 문제가 있어, 5개묶음과 정확히 1:1 대응되는 현재의 1~5위·1:1:1:1:1로 최종 확정.)
| 5 | 자동구매 풀(lotto_combinations) 정리 | 조합생성 트리거(#2)가 돌 때마다 — **최근 2회차만 보관**, 그 이전 회차는 삭제 | `cleanup_old_lotto_combinations(keep_rounds=2)` (2026-09-06 수정 — 예전엔 이 규칙 없이 즉시 전부 삭제해서 1237~1240회차 데이터가 복구 불가능하게 삭제됨) | `marketing_db.py`, `combo_gen_worker.py` |
| 6 | 번개조합/안티·액땜조합 저장분(guest_generated_combos) 정리 | 조합생성 트리거(#2)가 돌 때마다 — **최근 2회차만 보관**(소스 구분 없이 draw_round 기준 통합), 그 이전 회차는 삭제 | `cleanup_old_guest_generated_combos(keep_rounds=2)` | `marketing_db.py`, `combo_gen_worker.py` |
| 7 | 자동구매 주문(guest_auto_orders/auto_orders) 정리 | 조합생성 트리거(#2)가 돌 때마다 — **최근 2회차만 보관**, 그 이전 회차는 삭제 (2026-09-07 신규 — 이전엔 화면 표시만 자르고 실제 삭제가 없었음) | `cleanup_old_guest_auto_orders(keep_rounds=2)`(marketing_db.py), `cleanup_old_auto_orders(keep_rounds=2)`(wallet_db.py) | `marketing_db.py`, `wallet_db.py`, `combo_gen_worker.py` |

## ⚠️ 유저 구매/저장 기록 공통 4대 원칙 (2026-09-07, 여러 번 반복 확인됨 — 절대 잊지 말 것)
**적용 대상 3곳: 자동구매 / 번개조합 / 안티·액땜조합 — 세 화면 모두 동일하게:**
1. 세 화면 공통 적용 (화면마다 다르게 가면 안 됨)
2. 구매/생성하면 **자동 저장**
3. 추첨 후 **적중내역 자동 마킹**
4. 유저의 구매/저장 기록은 **최근 2회차만 보관**, 다음 회차 진행 시기부터는 **자동 삭제**

**2026-09-07 코드 대조 결과 → 같은 날 수정 완료**:
- 번개조합/안티·액땜조합(`guest_generated_combos`) — 4개 원칙 전부 충족 확인.
- **자동구매는 4번이 미충족이었음** — `guest_auto_orders`/`auto_orders`는 실제 삭제 로직이 없고, `page_auto.py`의 `MAX_HISTORY_ROUNDS=2`로 **화면 표시만** 최근 2회차로 자르고 있었음. `cleanup_old_guest_auto_orders()`(marketing_db.py) / `cleanup_old_auto_orders()`(wallet_db.py) 신규 추가로 수정 완료 — 표 6의 정리 로직과 함께 `combo_gen_worker.py`가 매주 호출. 운영 DB에 쌓여있던 174건도 그 자리에서 정리함.

## 답변 대기 중 (2026-09-07)
- (해결됨) **메인화면 캐릭터 주변 유력수 6개**: 관리 기능(`admin_dashboard.py`의 "🔧 메인화면 유력수 보정")이 삭제된 게 아니라 "필터 관리 → ① 기본 탭" 안에 파묻혀 있던 것 — 사용자가 위치를 직접 확인, 옮기지 않고 그대로 유지하기로 확정.
- (해결됨) **추출·구매 배정 비율**: 2026-09-08엔 "1위/2위/3위그룹 2:2:1"이었으나, 2026-09-13 사용자 지시로 "1~5위그룹 1:1:1:1:1(각 20%), 5% 추출"로 최종 개편(위 "핵심 개념" 참고) — 추출(`combo_gen_worker.py`)과 배정(`marketing_db.py`) 양쪽 다 반영 완료, 마스크↔티어 라운드트립 테스트 및 1242회차 실제 조합 생성(`final_count=1,064,755`)으로 재검증 완료.

## 확인이 필요한 잠재 후보 (아직 시간기반인지 미검증)
- 고급필터 구독 만료(`subscriptions.expires_at`) — 구독 만료 자체는 시간 기반이지만 "매 시각 도는 자동화"는 아니고 조회 시점에 비교하는 방식으로 보임(추가 확인 필요 시 여기 업데이트)

## 점검 습관
- 이 표에 있는 항목 중 하나를 건드리는 작업을 할 때는, 고치기 전에 이 표부터 다시 읽고 실제 코드와 대조할 것.
- 이 표 자체가 실제 코드와 어긋나 있지 않은지, 새 시간기반 로직을 추가할 때마다 같이 갱신할 것.
