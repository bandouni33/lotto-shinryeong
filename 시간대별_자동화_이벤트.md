# 로또신령 — 시간대별 자동화 이벤트 모음

여러 파일에 흩어져 있어서 하나가 조용히 스킵돼도 눈치채기 어려운 "시간/요일 기반" 로직을 한 곳에 모아둔다. 새로 추가되는 시간 기반 자동화는 반드시 여기에도 기록할 것. (2026-09-06, 1241회차 조합생성 트리거가 조건 버그로 조용히 스킵된 사건을 계기로 작성)

## 📌 고정 참고 — 적립금/배포방식 (2026-09-08 확정, 항상 이 기준으로 볼 것)

| 화면 | 필요 적립금 | 배포방식 |
|---|---|---|
| 자동구매 | 5개 **50P**(10P/개) | 4차 필터 통과 배포용 저장본에서 **1위/2위/3위그룹 2:2:1** 비율로 배정 |
| 번개조합 | 5개 **50P**(10P/게임) | 고정·삭제·행운수 조건 그대로 적용. 배포용 저장본에 조건 일치 조합 있으면 우선 배정, 부족분은 신규생성 — **미구현·설계 대기** |
| 전체·개별리셋(안티/액땜) | 5개 선택 시 **100P**(선택수×2×10P — 개별+전체 동시 생성) | 스캔번호 반대조건 그대로 적용. 배포용 저장본에 조건 일치 조합 있으면 우선 배정, 부족분은 신규생성 — **미구현·설계 대기** |

- 2026-09-08 정정: `wallet_db.AUTO_COST_PER_UNIT`이 100P로 잘못 들어가 있었음(사용자 확인: 실제는 10P/개, 번개조합과 동일) — 10으로 정정, `legal_notices.py`의 PRICING/안내문구도 동기화 완료.
- "배포용 저장본 우선 배정" 항목(번개조합·전체/개별리셋)은 아직 코드에 없음 — 설계 확정 후 구현 예정.

| # | 이벤트 | 조건(요일/시각) | 실행 방식 | 관련 파일 |
|---|---|---|---|---|
| 1 | 당첨번호 자동동기화 | **방문자 무관, 매시 정각(UTC) 상시 실행** — 2026-09-07부터 GitHub Actions로 전환(기존 방문자 트리거 방식은 병행 유지) | GitHub Actions 스케줄(`.github/workflows/hourly_draw_sync.yml`)이 `hourly_draw_sync.py` 실행 → `sync_latest_from_dhlottery()`. 실제 작동엔 저장소 시크릿(`TURSO_DATABASE_URL`,`TURSO_AUTH_TOKEN`) 등록 필요(2026-09-07 기준 등록 대기) | `hourly_draw_sync.py`, `.github/workflows/hourly_draw_sync.yml`, `draw_results_db.py` |
| 2 | 다음 회차 조합 생성 트리거 | **매주 일요일 14:00~14:59 KST**, 30분 쿨다운, 누군가 앱 방문 시 체크 | `subprocess.Popen`으로 `combo_gen_worker.py` 백그라운드 실행(3~5분 소요). 4차 필터 통과 조합 중 10%를 **1위/2위/3위그룹 2:2:1 비율**로 추출·저장(아래 "핵심 개념" 참고). 회차별 "확인용" 로컬 사본을 `lotto-app/{회차}회차/조합_확인용.csv`로 별도 저장(배포용 Turso 저장본과 별개, git 미포함) | `combo_gen_trigger.py`(트리거), `combo_gen_worker.py`(실제 생성 + `RANK_TIER_RATIO`, `extract_sample_by_rank_tier_ratio`, `save_local_verification_copy`), `app.py`(진입점) |
| 3 | 자동구매 판매 가능 시간 | **화요일 09:00 ~ 토요일 19:55 KST** — 이 시간 외엔 자동구매 자체가 막힘 | `page_auto.py`의 시간 체크 함수가 매 렌더마다 확인 | `page_auto.py` (라인 29, 34, 43 부근) |
| 4 | 구매조합 배분(1묶음=5개) | 시간 조건 아님(참고용) — 5개 구매 단위마다 **1위/2위/3위그룹 2:2:1**로 배정(10개면 4:4:2) | 구매 시점에 `allocate_lotto_combinations_random_sequential()` 호출. 2026-09-07 편향 버그 수정: `_pick_spread_indices`가 그룹 내 앞쪽을 뒤쪽보다 최대 6배 더 자주 뽑던 편향을 시뮬레이션으로 확인 후 `random.sample` 기반 균등추출로 교체 | `marketing_db.py`(`RANK_TIER_RATIO`, `_rank_tier_quota`, `_pick_bundle_candidates`) |

### 핵심 개념: 1위/2위/3위그룹 정의 (2026-09-08 최종 확정, 여러 차례 정정 끝에 정리됨)
그 회차의 격차순위(gap_order) 1~3위 숫자(예: 1위=34, 2위=18, 3위=5)를 기준으로, 4차 필터를 통과한 모든 조합은 아래 3개 그룹 중 **정확히 하나**에만 속한다(서로 안 겹치고, 합치면 전체와 같음):
- **1위그룹**: 34 포함 (18·5는 있어도 되고 없어도 됨)
- **2위그룹**: 18 포함, **34는 제외**
- **3위그룹**: 5 포함, **34·18 둘 다 제외**

이 3그룹 비율(2:2:1)은 **① 매 회차 10% 추출 시**(위 표 #2)와 **② 구매 시 배정 시**(위 표 #4) 양쪽에 **똑같이** 적용돼야 한다 — 둘 중 하나만 다른 비율이면 특정 그룹이 먼저 바닥나 "정확히 배정" 원칙이 깨진다. (이전엔 "{1,2위}/{1,3위}/{1,2,3위}/{2,3위}/{3위만} 5개 마스크 각 1개씩"이었으나, 겹침 마스크가 후보 풀에서 극히 드물어(1241회차 실측: 특정 마스크 0.4%) 재고가 금방 바닥나는 문제가 있었음 — 사용자 본인 텍스트 착오였던 것으로 확인돼 이 3그룹·2:2:1 규칙으로 정정.)
| 5 | 자동구매 풀(lotto_combinations) 정리 | 조합생성 트리거(#2)가 돌 때마다 — **최근 2회차만 보관**, 그 이전 회차는 삭제 | `cleanup_old_lotto_combinations(keep_rounds=2)` (2026-09-06 수정 — 예전엔 이 규칙 없이 즉시 전부 삭제해서 1237~1240회차 데이터가 복구 불가능하게 삭제됨) | `marketing_db.py`, `combo_gen_worker.py` |
| 6 | 번개조합/안티·액땜조합 저장분(guest_generated_combos) 정리 | 조합생성 트리거(#2)가 돌 때마다 — **최근 2회차만 보관**(소스 구분 없이 draw_round 기준 통합), 그 이전 회차는 삭제 | `cleanup_old_guest_generated_combos(keep_rounds=2)` | `marketing_db.py`, `combo_gen_worker.py` |
| 7 | 자동구매 주문(guest_auto_orders/auto_orders) 정리 | 조합생성 트리거(#2)가 돌 때마다 — **최근 2회차만 보관**, 그 이전 회차는 삭제 (2026-09-07 신규 — 이전엔 화면 표시만 자르고 실제 삭제가 없었음) | `cleanup_old_guest_auto_orders(keep_rounds=2)`(marketing_db.py), `cleanup_old_auto_orders(keep_rounds=2)`(wallet_db.py) | `marketing_db.py`, `wallet_db.py`, `combo_gen_worker.py` |

## ⚠️ 유저 구매/저장 기록 공통 4대 원칙 (2026-09-07, 여러 번 반복 확인됨 — 절대 잊지 말 것)
**적용 대상 3곳: 자동구매 / 번개조합 / 안티·액땜조합 — 세 화면 모두 동일하게:**
1. 세 화면 공통 적용 (화면마다 다르게 가면 안 됨)
2. 구매/생성하면 **자동 저장**
3. 추첨 후 **적중내역 자동 마킹**
4. 유저의 구매/저장 기록은 **최근 2회차만 보관**, 다음 회차 진행 시기부터는 **자동 삭제**

**2026-09-07 코드 대조 결과 → 같은 날 수정 완료**:
- 번개조합/안티·액땜조합(`guest_generated_combos`) — 4개 원칙 전부 충족 확인.
- **자동구매는 4번이 미충족이었음** — `guest_auto_orders`/`auto_orders`는 실제 삭제 로직이 없고, `page_auto.py`의 `MAX_HISTORY_ROUNDS=2`로 **화면 표시만** 최근 2회차로 자르고 있었음. `cleanup_old_guest_auto_orders()`(marketing_db.py) / `cleanup_old_auto_orders()`(wallet_db.py) 신규 추가로 수정 완료 — 표 6의 정리 로직과 함께 `combo_gen_worker.py`가 매주 호출. 운영 DB에 쌓여있던 174건도 그 자리에서 정리함.

## 답변 대기 중 (2026-09-07)
- (해결됨) **메인화면 캐릭터 주변 유력수 6개**: 관리 기능(`admin_dashboard.py`의 "🔧 메인화면 유력수 보정")이 삭제된 게 아니라 "필터 관리 → ① 기본 탭" 안에 파묻혀 있던 것 — 사용자가 위치를 직접 확인, 옮기지 않고 그대로 유지하기로 확정.
- (해결됨) **10% 추출·구매 배정 비율**: "1위/2위/3위그룹 2:2:1" 규칙으로 최종 정정 완료(위 "핵심 개념" 참고) — 추출(`combo_gen_worker.py`)과 배정(`marketing_db.py`) 양쪽 다 반영, 격리 테스트로 5개→2:2:1·10개→4:4:2 확인 완료.

## 확인이 필요한 잠재 후보 (아직 시간기반인지 미검증)
- 고급필터 구독 만료(`subscriptions.expires_at`) — 구독 만료 자체는 시간 기반이지만 "매 시각 도는 자동화"는 아니고 조회 시점에 비교하는 방식으로 보임(추가 확인 필요 시 여기 업데이트)

## 점검 습관
- 이 표에 있는 항목 중 하나를 건드리는 작업을 할 때는, 고치기 전에 이 표부터 다시 읽고 실제 코드와 대조할 것.
- 이 표 자체가 실제 코드와 어긋나 있지 않은지, 새 시간기반 로직을 추가할 때마다 같이 갱신할 것.
