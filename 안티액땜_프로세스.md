# 안티·액땜조합(전체·개별리셋) — 전체 프로세스 통합 정리

`page_hedge.py`, `wallet_db.py`, `marketing_db.py`, `legal_notices.py`에 흩어진 규칙을 한 곳에 모은다. (2026-09-09 작성)

## 1. 진입 조건

| 게이트 | 조건 | 실패 시 |
|---|---|---|
| 판매 가능 시간 | **화 09:00 ~ 토 19:55 (KST)** — 자동구매와 동일(2026-09-09 확정, `sales_window.py` 공용 모듈) | `SALES_WINDOW_BANNER` 안내문, "조합시작" 클릭 시 + 다이얼로그 확정 시 이중 체크 |
| 로그인 | `ensure_member_or_banner()` — 세션당 배너 1회 | 배너만 노출 |
| 적립금 확인 안내 | `points_notice_dialog("hedge", quantity=pending_count*2)` — 세션당 1회 | (안내만) |

## 2. 가격 — 다른 화면과 계산식이 다름 주의

**10P / 조합**이지만, 조합시작 한 번으로 **개별리셋 + 전체리셋을 항상 동시에** 생성한다(2026-08-27 확정) — 그래서 실제 차감은 **선택 수 × 2 × 10P**.
- 예: 5개 선택 → 개별 5 + 전체 5 = 10조합 = **100P**
- `calc_hedge_cost(combo_count)`에 넘기는 `combo_count`는 이미 2배 처리된 값(`pending_count * 2`) — 헷갈리기 쉬운 지점.

## 3. 입력 방식 — QR스캔 / 직접입력

- **직접입력**: 이 페이지 자체가 기본 상태라 별도 처리 불필요, 번호판에서 선택.
- **QR스캔**: 로또 용지 QR을 스캔 → 네이티브 앱이면 `window.ReactNativeWebView.postMessage`로 네이티브 카메라 트리거, 실패 시(브라우저 등) URL(`?page=hedge&qrscan=1`)로 폴백.
  - **2026-09-09 수정**: 이 폴백 URL이 고정 문자열이라 gid를 지워버려 게스트ID가 끊기는 원인이 됐음 — 기존 쿼리스트링(gid 포함) 보존하도록 수정.
  - **2026-09-09 수정**: 2026-08-22에 붙였던 실기기 디버깅용 배너("객체 자체가 없음 | Mozilla/5.0 ..." 등 개발자 진단 문구가 화면에 빨간 줄로 노출)가 제거 안 되고 남아있어서, 일반 브라우저 접속자가 QR스캔을 누를 때마다 매번 이 문구가 보이고 있었음 — 제거 완료.

## 4. 조합 생성 — 배포용 저장본 우선 배정

- 스캔/입력된 번호들의 **반대 조건**을 그대로 적용해 조합 생성.
- **`_pool_priority_matches(condition, count)`** (2026-09-05 구현): 배포용 풀(`get_random_pool_combos`)에서 조건과 일치하는 조합을 우선 사용, 부족분만 신규 생성.
- 번호 확정되는 즉시(다이얼로그 닫히는 시점) 개별·전체 둘 다 자동저장(`save_guest_generated_combos`, source=`"anti"`/`"aekddaem"`) — 결과 화면 확인 후 별도 저장 버튼 클릭 불필요(2026-08-27부터).
- **차감 순서 (2026-09-10, "실패 시 환불" 정책 통일):** `_hedge_dialog_close`에서 ① 잔액 확인 → ② `generate_anti_combinations`/`generate_aekddaem_combinations` 실행 → ③ **둘 다 목표 개수를 채웠을 때만** `deduct_after_result`로 차감. 필터가 조합 수를 못 채우거나 예외면 차감하지 않고 "조합 생성에 실패했습니다. 적립금은 차감되지 않았습니다." 안내.
- 저장 순서: anti(개별) 먼저 → aekddaem(전체) 나중 — 저장내역 화면에서 "전체가 위, 개별이 아래"로 보이게 하려는 의도적 순서.

## 5. 공통 4대 원칙 · 게스트ID 안정화

`자동구매_프로세스.md` 7·8번과 동일:
- 자동저장 / 적중 자동마킹 / 최근 2회차만 보관(`cleanup_old_guest_generated_combos(keep_rounds=2)`)
- 적립금 부족 시 통합 충전창(2026-09-08)
- 내부링크 gid 동봉 + 카카오 state 복원 + localStorage 백업 (2026-09-09)

## 6. 아직 안 끝난 것

(현재 없음 — 판매시간대 건은 2026-09-09에 해결됨)

## 조합생성 완료 안내 · 로그인 배너 위치 (2026-09-11, 4화면 통일)

- "조합생성이 완료되었습니다" 계열 안내를 조합시작 버튼 바로 밑에 표시(`wallet_ui.render_generation_complete_notice("hedge")`). 문구는 `legal_notices.GENERATION_COMPLETE_NOTICES`에서만 관리 — 자동구매·번개조합·안티액땜·타로 4화면 동일 패턴, 바꿀 땐 거기 한 곳만.
- 로그인 배너("카카오로 시작하기")는 화면 어디서 열려도 항상 최상단으로 1회 자동 스크롤된 뒤 나타난다(`wallet_ui.open_auth_banner`/`render_auth_banner`) — 유저가 버튼을 눌렀는데 배너가 안 보여 혼란스러운 문제 방지.
